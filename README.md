# Asynchronous-JavaScript-RESTful-APIs
Asynchronous JavaScript lets your code fetch data in the background without stopping the page. RESTful APIs are web services that provide data (like weather, news, or users) in a structured format.
